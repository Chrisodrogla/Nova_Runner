name: Generator

on:
    workflow_dispatch:
#    workflow_run:
#      workflows: ["Attributes"]
#      types:
#        - completed
# on:
#   workflow_dispatch: # This allows manual triggering of the workflow

  # schedule:
  # - cron: "0 0 * * *"  # This runs the workflow every day at midnight (UTC)

jobs:
  run_python_script:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Run Python script

      run: python scraper/scraper/Listing_Url/listing_url_generator.py

    - name: Commit and push changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
        git add scraper/scraper/Listing_Url/json_file/final_rental_link.json
        git commit -m 'Update final_rental_link.json'
        git push
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    - name: Commit and push changes

#      run: |
#        git config --global user.name 'github-actions[bot]'
#        git config --global user.email '41898282+github-actions[bot]@users.noreply.github.com'
#        git add /final_rental_link.json
#        git commit -m 'Update final_rental_link.json with latest data'
#        git push
#      env:
#        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
